export type Photo = {
  url: string;
};

export type TitleLogo = {
  textLeft: string;
  textRight: string;
};

export type TitleLogoDatabase = {
  id: string;
  textLeft: string;
  textRight: string;
};
