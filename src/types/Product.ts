export type Product = {
  category: string;
  name: string;
  unity: boolean;
  price: number;
  expense: boolean;
}

export type ProductDatabase = {
  category: string;
  name: string;
  unity: boolean;
  price: number;
  expense: boolean;
  id: string;
}